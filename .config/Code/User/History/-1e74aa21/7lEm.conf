context.modules = [
    {
        name = libpipewire-module-loopback
        args = {
            node.description = "Browser (Virtual)"
            capture.props = {
                node.name = "sink-browser"
                media.class = "Audio/Sink"
                audio.position = [ FL FR ]
            }
            playback.props = {
                node.name = "sink-browser-to-headset"
                audio.position = [ FL FR ]
                node.passive = true
                stream.dont-remix = true           
            }
        }
    }
    {
        name = libpipewire-module-loopback
        args = {
            node.description = "Spotify (Virtual)"
            capture.props = {
                node.name = "sink-spotify"
                media.class = "Audio/Sink"
                audio.position = [ FL FR ]
            }
            playback.props = {
                node.name = "sink-spotify-to-headset"
                audio.position = [ FL FR ]
                node.passive = true
                stream.dont-remix = true           
            }
        }
    }
    {
        name = libpipewire-module-loopback
        args = {
            node.description = "Discord (Virtual)"
            capture.props = {
                node.name = "sink-discord"
                media.class = "Audio/Sink"
                audio.position = [ FL FR ]
            }
            playback.props = {
                node.name = "sink-discord-to-headset"
                audio.position = [ FL FR ]
                node.passive = true
                stream.dont-remix = true           
            }
        }
    }
    {
        name = libpipewire-module-loopback
        args = {
            node.description = "Win System (Virtual)"
            audio.position   = [ FL FR ]

            capture.props = {
                # <<< Pegando o SOURCE vindo do VBAN
                node.target = "vban-win-system-audio"
                node.passive = true
            }

            playback.props = {
                # <<< Esse output vira um MIC VIRTUAL LIMPO
                media.class      = "Audio/Source"
                node.name        = "win-system-virtual-input"
                node.description = "WIN_SYSTEM Virtual Input (for OBS)"
            }
        }
    }
]